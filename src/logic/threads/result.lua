local net = require "net"

-- TODO: wait for readers connect
-- TODO: wait for workers connect

-- local sock = assert(net.unix.socket(1))

-- trace(assert(sock:bind("0.0.0.0", 12000):listen()))

-- trace(sock:accept(1))
